# Magic BI — MVP Semana 1
# Django 5.2 LTS + DRF + Celery, conforme docs/AgenteRotinaContabil-arquitetura-tecnica.md
django~=5.2
djangorestframework
celery[redis]
redis
django-environ
pydantic-ai-slim[groq]  # orquestração Opção A (docs/magicbi-hermes-comunicador.md §7) sobre Groq
groq  # cliente direto p/ transcrição de áudio (Whisper) — D6, apps/channel_whatsapp/transcricao.py
httpx
structlog
psycopg[binary]  # Postgres — único banco suportado (dev local ou docker-compose)
cryptography  # cifra segredos por campo + assina a DPS e extrai o par PEM do .pfx (mTLS)
nfelib  # bindings gerados do XSD OFICIAL da NFS-e Nacional — apps/fiscal/dps.py
signxml  # XMLDSig da DPS (enveloped, RSA-SHA1 + C14N 1.0 — fixados pelo XSD da NFS-e)
lxml  # árvore XML usada na assinatura
pyjwt  # Magic Link do wa_id↔CNPJ (apps/security) — docs/magicbi-seguranca-sessao.md
django-sesame  # Magic Link de login do painel/Grimório (contador) — caso diferente do de cima
Pillow  # upload de logo do escritório no painel (apps/painel) — SaaS multi-tenant, nada de marca hardcoded
django-unfold  # tema moderno do admin (Tailwind) — feedback "restilizar o admin", ver config/settings.py UNFOLD
boto3  # cliente S3 — fala com MinIO (auto-hospedado) e com a AWS pela MESMA API; a troca é de variável de ambiente, não de código
pypdf  # camada de texto do PDF (apps/documentos/extracao.py) — leitura exata quando o PDF veio de sistema
pypdfium2  # rasteriza o PDF que saiu do scanner (é uma foto embrulhada) — wheel, sem pacote de sistema
pyzbar  # código de barras e QR da imagem: DECODIFICA (falha em vez de errar) — precisa de libzbar0, ver Dockerfile
pytesseract  # OCR, o degrau que fica ABAIXO do limiar de propósito — precisa de tesseract-ocr, ver Dockerfile
pytest
pytest-django
sentry-sdk  # captura de exceção — só ativa com SENTRY_DSN (ver apps/observabilidade/sentry.py)
