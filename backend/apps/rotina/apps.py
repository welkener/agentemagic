from django.apps import AppConfig


class RotinaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.rotina"
    verbose_name = "Rotina contábil (guias, obrigações, certidões)"
